[project]
name = "spec-decoding"
version = "0.1.0"
description = "Speculative decoding for low-resource language"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0.0",
    "transformers>=4.36.0",
    "sacrebleu>=2.0.0",
    "tqdm>=4.0.0",
    "datasets>=4.4.1",
    "ruff>=0.14.7",
]
